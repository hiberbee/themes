plugins {
  id("idea")
  id("java")
  id("org.jetbrains.gradle.plugin.idea-ext") version "1.1.4"
  id("com.github.ben-manes.versions") version "0.42.0"
  id("org.jetbrains.intellij") version "1.6.0-SNAPSHOT"
}

group = "com.hiberbee.themes"
version = System.getenv().getOrDefault("APP_VERSION", new Date().format('yyyy.M.d.HHmm'))

repositories {
  mavenCentral()
}

intellij {
  pluginName = "Hiberbee theme"
  type = "IC"
  version = "LATEST-EAP-SNAPSHOT"
  configureDefaultDependencies = true
  updateSinceUntilBuild = false
  downloadSources = true
}

idea {
  project {
    settings {
      copyright {
        useDefault = "MIT"
        profiles {
          MIT {
            notice = file("LICENSE").readLines().join("\n")
          }
        }
      }
    }
  }
}

publishPlugin {
  setToken("perm:${System.getenv("JETBRAINS_TOKEN")}")
}

patchPluginXml {
  version(project.version)
}
